/* tslint:disable */
/* eslint-disable */
/**
 * January AI Partner API
 * Build food and metabolic intelligence into your product with the January API — one API for understanding what people eat and how food may affect them.  Use it to **search foods** by name, barcode, natural language, restaurant, or menu item; **scan meals from photos** and correct the results in plain English; **log food** and retrieve a user\'s history; **recommend healthier alternatives**; and **predict glucose response** to a meal — no sensor required.
 *
 * The version of the OpenAPI document: 1.2
 * Contact: umer@january.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type CorrectPhotoScanBody,
    CorrectPhotoScanBodyFromJSON,
    CorrectPhotoScanBodyToJSON,
} from '../models/CorrectPhotoScanBody.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type PhotoScan,
    PhotoScanFromJSON,
    PhotoScanToJSON,
} from '../models/PhotoScan.js';
import {
    type ScanFoodPhotoBody,
    ScanFoodPhotoBodyFromJSON,
    ScanFoodPhotoBodyToJSON,
} from '../models/ScanFoodPhotoBody.js';

export interface CorrectPhotoScanRequest {
    correctPhotoScanBody: CorrectPhotoScanBody;
    xEndUserId?: string;
}

export interface ScanFoodPhotoRequest {
    scanFoodPhotoBody: ScanFoodPhotoBody;
    xEndUserId?: string;
}

/**
 *
 */
export class PhotoScanningApi extends runtime.BaseAPI {

    /**
     * Creates request options for correctPhotoScan without sending the request
     */
    async correctPhotoScanRequestOpts(requestParameters: CorrectPhotoScanRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['correctPhotoScanBody'] == null) {
            throw new runtime.RequiredError(
                'correctPhotoScanBody',
                'Required parameter "correctPhotoScanBody" was null or undefined when calling correctPhotoScan().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xEndUserId'] != null) {
            headerParameters['x-end-user-id'] = String(requestParameters['xEndUserId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1.2/meal-scan/fix-ai`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CorrectPhotoScanBodyToJSON(requestParameters['correctPhotoScanBody']),
        };
    }

    /**
     * Refines a meal-scan result. Send back `meal_name` and `detections` exactly as the scan returned them, plus `user_input` describing the correction; the response is a corrected scan result with recalculated totals.
     * Correct a scan with a plain-English instruction
     */
    async correctPhotoScanRaw(requestParameters: CorrectPhotoScanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PhotoScan>> {
        const requestOptions = await this.correctPhotoScanRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PhotoScanFromJSON(jsonValue));
    }

    /**
     * Refines a meal-scan result. Send back `meal_name` and `detections` exactly as the scan returned them, plus `user_input` describing the correction; the response is a corrected scan result with recalculated totals.
     * Correct a scan with a plain-English instruction
     */
    async correctPhotoScan(requestParameters: CorrectPhotoScanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PhotoScan> {
        const response = await this.correctPhotoScanRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for scanFoodPhoto without sending the request
     */
    async scanFoodPhotoRequestOpts(requestParameters: ScanFoodPhotoRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['scanFoodPhotoBody'] == null) {
            throw new runtime.RequiredError(
                'scanFoodPhotoBody',
                'Required parameter "scanFoodPhotoBody" was null or undefined when calling scanFoodPhoto().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xEndUserId'] != null) {
            headerParameters['x-end-user-id'] = String(requestParameters['xEndUserId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1.2/meal-scan`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScanFoodPhotoBodyToJSON(requestParameters['scanFoodPhotoBody']),
        };
    }

    /**
     * Analyzes a meal photo and returns the detected foods with their nutrition and an aggregated total. `image` accepts either an http(s) URL or a base64 data URI. Analysis can take tens of seconds for complex meals.
     * Detect foods and nutrition in a meal photo
     */
    async scanFoodPhotoRaw(requestParameters: ScanFoodPhotoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PhotoScan>> {
        const requestOptions = await this.scanFoodPhotoRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PhotoScanFromJSON(jsonValue));
    }

    /**
     * Analyzes a meal photo and returns the detected foods with their nutrition and an aggregated total. `image` accepts either an http(s) URL or a base64 data URI. Analysis can take tens of seconds for complex meals.
     * Detect foods and nutrition in a meal photo
     */
    async scanFoodPhoto(requestParameters: ScanFoodPhotoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PhotoScan> {
        const response = await this.scanFoodPhotoRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
