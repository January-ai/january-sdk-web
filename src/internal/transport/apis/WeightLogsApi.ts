/* tslint:disable */
/* eslint-disable */
/**
 * January AI - Nutrition Intelligence APIs
 * Build food and metabolic intelligence into your product — one API for understanding what people eat and how food may affect them.  **Clinical-grade precision, consumer-grade experiences.** January builds the infrastructure underneath the product: turning messy health and nutrition data into reliable intelligence, so your team spends its time on the experience instead of the foundation.  **Security & compliance** — **SOC 2 Type II** · **HIPAA-aligned practices** · **BAA** and **Zero Data Retention (ZDR)** available  **What you can build** - **Scan food** — photo and text food recognition: detect foods and nutrition, then correct results conversationally - **Search the food database** — by name or barcode — and get healthier alternatives for any food - **Log food, water and weight** — per-user diaries with day-range queries - **Predict glucose response** to any meal — no sensor required  **Getting started** 1. Create an API key in the [Developer Dashboard](https://dashboard.january.ai) — the full key is shown once, at creation. 2. Send it as `Authorization: Bearer sk-…` — click **Authorize** here to make every example below a live request. 3. On the endpoints that read or write an end user\'s logs, say whose with the `January-End-User-ID` header — each one documents it. No other endpoint takes it: everything else either asks the shared food database a question or works on the body you send, and neither depends on who the food is for.  **Calling from a mobile app** — your `sk-` key must never ship inside an app. Instead, exchange it on your backend for a *client token*: a credential that lasts up to two hours, acts as exactly one of your end users, and carries only the scopes you grant it (see the **authentication** section). Your app then calls these endpoints directly, with no proxy of your own in the request path. Both credentials travel in the same `Authorization: Bearer` header, and every endpoint below opens by saying which it accepts — **API key or client token**, or **API key only**.  **Support** — [support@january.ai](mailto:support@january.ai) · [Discord community](https://discord.gg/cYQeh3UnC) · [docs.january.ai](https://docs.january.ai)
 *
 * The version of the OpenAPI document: 1.2
 * Contact: support@january.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type CreateWeightLogBody,
    CreateWeightLogBodyFromJSON,
    CreateWeightLogBodyToJSON,
} from '../models/CreateWeightLogBody.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type ListWeightLogsResponse,
    ListWeightLogsResponseFromJSON,
    ListWeightLogsResponseToJSON,
} from '../models/ListWeightLogsResponse.js';
import {
    type WeightLog,
    WeightLogFromJSON,
    WeightLogToJSON,
} from '../models/WeightLog.js';

export interface CreateWeightLogRequest {
    createWeightLogBody: CreateWeightLogBody;
    januaryEndUserID?: string;
}

export interface ListWeightLogsRequest {
    startDate: Date;
    endDate: Date;
    timezone: string;
    januaryEndUserID?: string;
}

/**
 *
 */
export class WeightLogsApi extends runtime.BaseAPI {

    /**
     * Creates request options for createWeightLog without sending the request
     */
    async createWeightLogRequestOpts(requestParameters: CreateWeightLogRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createWeightLogBody'] == null) {
            throw new runtime.RequiredError(
                'createWeightLogBody',
                'Required parameter "createWeightLogBody" was null or undefined when calling createWeightLog().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['januaryEndUserID'] != null) {
            headerParameters['January-End-User-ID'] = String(requestParameters['januaryEndUserID']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1.2/weight-logs`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateWeightLogBodyToJSON(requestParameters['createWeightLogBody']),
        };
    }

    /**
     * **API key or client token.**  Records one weight measurement, in `lb` (10–1000) or `kg` (4.5–453.6). Every measurement is kept; listing shows one per local day — the latest by `measured_at` — so logging again later the same day replaces what that day shows. Not idempotent: a retried create records the measurement twice, which listing then shows once.  Callable with a client token carrying the `weight_logs:write` scope.
     * Log a weight for a user
     */
    async createWeightLogRaw(requestParameters: CreateWeightLogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WeightLog>> {
        const requestOptions = await this.createWeightLogRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WeightLogFromJSON(jsonValue));
    }

    /**
     * **API key or client token.**  Records one weight measurement, in `lb` (10–1000) or `kg` (4.5–453.6). Every measurement is kept; listing shows one per local day — the latest by `measured_at` — so logging again later the same day replaces what that day shows. Not idempotent: a retried create records the measurement twice, which listing then shows once.  Callable with a client token carrying the `weight_logs:write` scope.
     * Log a weight for a user
     */
    async createWeightLog(requestParameters: CreateWeightLogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WeightLog> {
        const response = await this.createWeightLogRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listWeightLogs without sending the request
     */
    async listWeightLogsRequestOpts(requestParameters: ListWeightLogsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['startDate'] == null) {
            throw new runtime.RequiredError(
                'startDate',
                'Required parameter "startDate" was null or undefined when calling listWeightLogs().'
            );
        }

        if (requestParameters['endDate'] == null) {
            throw new runtime.RequiredError(
                'endDate',
                'Required parameter "endDate" was null or undefined when calling listWeightLogs().'
            );
        }

        if (requestParameters['timezone'] == null) {
            throw new runtime.RequiredError(
                'timezone',
                'Required parameter "timezone" was null or undefined when calling listWeightLogs().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['startDate'] != null) {
            queryParameters['start_date'] = (requestParameters['startDate'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['endDate'] != null) {
            queryParameters['end_date'] = (requestParameters['endDate'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['timezone'] != null) {
            queryParameters['timezone'] = requestParameters['timezone'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['januaryEndUserID'] != null) {
            headerParameters['January-End-User-ID'] = String(requestParameters['januaryEndUserID']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1.2/weight-logs`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * **API key or client token.**  Returns one weight per day between `start_date` and `end_date` (both inclusive local calendar dates in `timezone`), oldest first. Only days with a logged weight appear; when several were logged on one day, the one with the latest `measured_at` is returned, in the unit it was logged in. At most 100 days are returned — when more match, the most recent 100. `start_date` may reach back at most 5 years from today in `timezone`. An empty list is a valid result.  Callable with a client token carrying the `weight_logs:read` scope.
     * List a user\'s daily weights in a date range
     */
    async listWeightLogsRaw(requestParameters: ListWeightLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListWeightLogsResponse>> {
        const requestOptions = await this.listWeightLogsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListWeightLogsResponseFromJSON(jsonValue));
    }

    /**
     * **API key or client token.**  Returns one weight per day between `start_date` and `end_date` (both inclusive local calendar dates in `timezone`), oldest first. Only days with a logged weight appear; when several were logged on one day, the one with the latest `measured_at` is returned, in the unit it was logged in. At most 100 days are returned — when more match, the most recent 100. `start_date` may reach back at most 5 years from today in `timezone`. An empty list is a valid result.  Callable with a client token carrying the `weight_logs:read` scope.
     * List a user\'s daily weights in a date range
     */
    async listWeightLogs(requestParameters: ListWeightLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListWeightLogsResponse> {
        const response = await this.listWeightLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
